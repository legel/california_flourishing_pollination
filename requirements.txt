# California Flourishing & Pollination — pinned dependencies
# torch / torchvision are installed separately with the CUDA 12.4 wheel index
# (see README; equivalent to: pip install torch==2.5.1 torchvision==0.20.1 --index-url https://download.pytorch.org/whl/cu124)

# Core scientific stack
numpy==2.1.3
pandas==2.2.3
pyarrow==18.1.0
scipy==1.14.1
scikit-learn==1.5.2

# Imaging
pillow==11.0.0
opencv-python==4.10.0.84

# Manifold + embeddings
umap-learn==0.5.7

# Transformers / DINOv3 — pinned to versions that ship the new `hf` CLI
transformers==5.9.0
tokenizers==0.22.2
huggingface_hub[cli,hf_transfer]==1.16.1
safetensors==0.4.5
accelerate==1.1.1

# HTTP / streaming
aiohttp==3.11.10
httpx==0.28.1
requests==2.32.3
tenacity==9.0.0

# Biodiversity APIs
pygbif==0.6.4

# Data validation + provenance
pydantic==2.10.3
python-slugify==8.0.4

# CLI + progress
typer==0.15.1
rich==13.9.4
tqdm==4.67.1

# Logging / config
pyyaml==6.0.2
structlog==24.4.0
